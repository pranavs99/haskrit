-- module Spec (main) where

-- import Unit.


-- data TestResult a
--     = Passed a
--     | Failed a a
--     deriving (Show, Eq)

-- runSuite :: String -> (String -> IO ())
-- runSuite ""

-- main :: IO ()
-- main = do
--     putStrLn ""
--     putStrLn "enter test suite:"
--     suite <- getLine
--     case runSuite suite of
--         Just tests -> run
